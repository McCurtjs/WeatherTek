
##
# ApplicationController base class - it's not currently being used for this
# project, but would be useful if this application had, say, user profiles to
# load by default.
class ApplicationController < ActionController::Base
end
